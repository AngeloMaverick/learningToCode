package classe;

public class ValorNulo {
	
	/*
		Classe pessoa
			Nome da pessoa string, peso double, método comer vai receber como parametro um objeto da classe comida
			pegar o peso da comida e acrescentar ao peso da pessoa
		classe comida
			nome, peso double.
		classe jantar
			main
			instanciar pessoa e 2 comida.
			pessoa deve comer a comida
		
	*/

	public static void main(String[] args) {

		String s1 = "";
		System.out.println(s1.concat("!!!!"));

		// 50% de probabilidade
		Data d1 = Math.random() > 0.5 ? new Data() : null;

		if (d1 != null) {
			d1.mes = 3;
			System.out.println(d1.obterDataFormatada());
		}

		String s2 = Math.random() > 0.5 ? "Opa" : null;

		if (s2 != null) {
			System.out.println(s2.concat("????"));
		}

	}
}
